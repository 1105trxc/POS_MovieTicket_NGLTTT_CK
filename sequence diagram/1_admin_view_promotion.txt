@startuml
title Sequence Diagram: Quản lý Khuyến Mãi - Xem danh sách

actor Admin as admin
participant "PromotionManagementPanel" as view
participant "PromotionController" as ctl
participant "PromotionServiceImpl" as svc
participant "PromotionRepository" as repo
database "Database" as db

admin -> view: Mở trang Quản lý Khuyến Mãi
view -> ctl: getAllPromotions()
ctl -> svc: getAllPromotions()
svc -> repo: findAll()
repo -> db: Query SELECT
db --> repo: Result List
repo --> svc: List<Promotion>
svc --> ctl: List<Promotion>
ctl --> view: Hiển thị lên JTable
@enduml
