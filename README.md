NHO CHAY CAI SCRIPTS NAY NHA, TAO MOI THEM MAY CAI NAY VO USER

ALTER TABLE `User` 
ADD COLUMN `Phone` VARCHAR(20),
ADD COLUMN `Email` VARCHAR(100),
ADD COLUMN `BirthDate` DATE,
ADD COLUMN `CCCD` VARCHAR(20) UNIQUE, -- Số CCCD
ADD COLUMN `Gender` VARCHAR(10);
